#! /usr/bin/env python
# encoding: utf-8

def configure(conf):
    conf.recurse(['3in1-sensor', '3in1-calibrator', 'pump-controller', 'test'])

def build(bld):
    bld.recurse(['3in1-sensor', '3in1-calibrator', 'pump-controller', 'test'])
